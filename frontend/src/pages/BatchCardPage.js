import BatchCard from '../components/BatchCard';

const BatchCardPage = () => {
  return (
    <div className="bg-gradient-to-r from-blue-500 to-purple-600 min-h-screen">
      <BatchCard />
    </div>
  );
};

export default BatchCardPage;